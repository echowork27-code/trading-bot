{
  "name": "ton-trading-bot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@telegram-apps/sdk": "^2.11.0"
  },
  "devDependencies": {
    "vite": "^6.1.0",
    "gh-pages": "^6.3.0"
  }
}
